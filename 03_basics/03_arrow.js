// a normal function can be defined as follows 

function greet(){
    console.log("Hello World")
}

// an arrow function is declared as follows

() => {} // This is an arrow func

// Passing args to this arrow function

// (num1,num2) => {
//     return num1+num2
// }

// Saving it to some variable

// let sum = (num1,num2) => {
//     return num1+num2
// }

// A more short hand for this is if we want we can remove the return keyword we can write it like this

// let sum = (num1, num2) => num1+num2
// or
// let sum = (num1, num2) => (num1+num2)
console.log(sum(5,7))
